
import { MathJaxContext, MathJax } from "better-react-mathjax";

const Materials = () => {
    return (

        <div className="flex flex-col shadow-md p-7 w-[75%] h-[50vh] h-auto rounded-2xl bg-white">
            <MathJaxContext>

                <p>Imagine dois relógios, cada um com apenas um ponteiro. O primeiro pode assumir 3 posições distintas, e o segundo, 5.</p>

            </MathJaxContext>
        </div>
    );
};

export default Materials;